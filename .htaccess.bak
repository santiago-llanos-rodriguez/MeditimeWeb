RewriteEngine On

# Quitar la extensión .html al acceder
RewriteCond %{REQUEST_FILENAME}.html -f
RewriteRule ^(.*?)/?$ $1.html [L]

# Opcional: redirigir a la URL sin .html cuando accedan con .html
RewriteCond %{THE_REQUEST} \s/+(.+?)\.html[\s?]
RewriteRule ^ %1 [R=301,L]
